#!/bin/bash

PATH="$PATH:/usr/X11R6/bin/"

USER='root'
#$RUNAS

DEPTH='16'

#GEOMETRY='800x600'
#GEOMETRY='1024x768'
GEOMETRY='1280x1024'

NAME='vncserver'
#AUTH='/var/run/slim.auth'
OPTIONS="-geometry $GEOMETRY -localhost -noxrecord -bg -forever -unixpw -noxfixes -noxdamage -chatwindow -localhost -xkb -display :0.0"

echo "Starting vncserver for user '$USER' on localhost:$DISPLAY"
exec "/usr/bin/x11vnc $OPTIONS"

exit 0
